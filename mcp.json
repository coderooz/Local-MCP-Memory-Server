{
  "mcpServers": {
    "local-mcp-memory": {
      "command": "node",
      "args": ["${cwd}/mcp-shim.js"],
      "cwd": "${cwd}",
      "env": {
        "MCP_PROJECT": "local-mcp-server",
        "MCP_SCOPE": "project",
        "NODE_ENV": "development"
      }
    }
  }
}
